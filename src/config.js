export const quizConfig = Object.freeze({
  mode: "practice",
  randomizeQuestions: true,
  randomizeAnswers: true,
  enableReview: true,
  showTimeTakenOnResults: true,
  persistSession: true,
  maxNameLength: 60,
  minImageOptions: 2,
  storageKey: "memgo.visual-vocabulary.v1"
});